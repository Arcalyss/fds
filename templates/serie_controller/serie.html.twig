{% extends 'base.html.twig' %}

{% block title %}série{% endblock %}
{% block body %}
<h1>vos {{nbSeries}} series favori</h1>
<div class="container overflow-hidden text-center">
  <div class="row gy-5">
    <div class="col-6">
      {% for serie in LesSeries %}
      <div class="p-3 border bg-light">
        <div class="card-body">
          <img src={{serie.image}} class="d-block w-100" >
          <h1> {{ serie.titre }} </h1>
          <p> {{ serie.duree }} </p>
          <p> {{ serie.premiereDiffusion| date("d/m/y") }} </p>
          <p> {{ serie.resume }} </p>
        </div>
      </div>
      {% endfor %}
    </div>
  </div>
</div>
{% endblock %}
